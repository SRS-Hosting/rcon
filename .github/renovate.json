{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "local>USA-RedDragon/renovate-configs",
        "local>USA-RedDragon/renovate-configs:golang"
    ],
    "packageRules": [
        {
            "description": "The go directive is a library compatibility floor, not a dependency: patch bumps force every consumer on an older toolchain to upgrade for no benefit.",
            "matchDatasources": ["golang-version"],
            "matchUpdateTypes": ["patch"],
            "enabled": false
        }
    ]
}
